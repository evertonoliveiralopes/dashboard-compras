from fastapi import UploadFile
import pandas as pd


def ler_arquivo(arquivo: UploadFile) -> pd.DataFrame:
    """
    Responsável por ler o arquivo enviado e retornar um DataFrame.
    A implementação será feita na próxima etapa.
    """
    raise NotImplementedError("Função ainda não implementada.")